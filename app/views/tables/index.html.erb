<p id="notice"><%= notice %></p>

<h1>Tables</h1>

<table class="table">
  <thead>
    <tr>
      <th>Id</th>
      <th>Hero image</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tables.each do |table| %>
      <tr>
        <td><%= table.id %></td>
        <td><%= image_tag table.hero_image, {class: "rounded img-fluid", width: "100px"} %></td>
        <td><%= table.title %></td>
        <td>
          <%= link_to 'Show', table, {class: "btn btn-primary btn-sm"} %>
        </td>
        <td>
          <%= link_to 'Edit', edit_table_path(table), {class: "btn btn-primary btn-sm"} %>
        </td>
        <td>
          <%= link_to 'Destroy', table, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" %></td>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Table', new_table_path, {class: "btn btn-primary"} %>
